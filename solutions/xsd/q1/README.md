Write an XML script to create an XML document containing an element Person with the sub-elements Age, Name, and Pincode. Write the equivalent XSD to validate the following restrictions:

Age should be between 10 and 20.

Name should accept only “Ram” or “Shyam.”

Pincode should be a 3-digit number.
